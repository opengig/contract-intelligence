pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{ts,tsx}"
      run: pnpm turbo run lint
    format:
      glob: "*.{ts,tsx,md,json}"
      run: pnpm prettier --write {staged_files} && git add {staged_files}
    test:
      glob: "*.{ts,tsx}"
      run: pnpm run test
